
import React from 'react';
import { Translations } from '../types';

interface UmamaProductsProps {
  t: Translations;
}

const UmamaProducts: React.FC<UmamaProductsProps> = () => {
  return null;
};

export default UmamaProducts;
